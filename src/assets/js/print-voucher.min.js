const escpos = require('escpos');

// Selecciona la impresora (puede ser USB, Serial, etc.)
const device = new escpos.USB(); // Cambia a Serial si es necesario
const printer = new escpos.Printer(device);

device.open(function() {
    printer
        .font('A')
        .align('ct')
        .style('b')
        .size(1, 1)
        .text('¡Hola, mundo!')
        .barcode('123456789012', 'EAN13')
        .cut()
        .close();
});
